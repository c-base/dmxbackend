how to check

cd clienttest/..
export DJANGO_SETTINGS_MODULE=dmxacl.settings
python:

from jsonrpc.proxy import ServiceProxy
s = ServiceProxy('http://localhost:8000/json/')
result = s.lightSync.pull()['result']
for lampe in result:
    lampe['blue'] = 255

s.lightSync.push(result)

